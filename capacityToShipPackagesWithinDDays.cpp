#include <iostream>
#include <vector>

class Solution {
  public:
    int shipWithinDays(std::vector<int>& weights, int days) {

    }
};

int main() {
  std::cout << "enter two number: " << std::endl;

  int v = 0, k = 0;

  std::cin >> v >> k;

  std::cout
      << "v is " << v
      << " and "
      << "k is " << k
      << " and total is "
      << v + k
      << std::endl; 

  return 0;
}
